# Manual do usuário — API bootstrap (stub)

**Status:** UI de produto ainda não disponível (fase Claude Design).

Enquanto isso, operações são feitas via API/Swagger (`/docs`).  
Quando a interface gráfica entrar, este manual será reescrito por tela.
